return {
    AnimationSpeed = 0.25, -- seconds
    AnimationStyle = Enum.EasingStyle.Sine, -- quad/quint/sine/etc
    AnimationDirection = Enum.EasingDirection.Out, -- in/inout/out
    CameraOffset = Vector3.new(0, 0, 30), -- sides/height/depth
    CameraFOV = 60, -- field of view
    CameraDeadzoneSize = Vector2.new(12, 6),
}
